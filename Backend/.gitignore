# Dependencies — never push node_modules, it's huge and regeneratable via npm install
node_modules/

# Environment variables — NEVER push this, it has your DB password, JWT secret, API keys
.env

# OS junk files
.DS_Store
Thumbs.db

# Editor config (optional — some teams include this, some don't)
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Build output (if you ever add a build step)
dist/
build/
